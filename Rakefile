require 'rubygems'
require 'bundler/setup'

require 'yard'

YARD::Rake::YardocTask.new do |t|
  t.files = ['lib/**/*.rb', 'features/step_definitions/*_steps.rb', 'features/**/*.feature', 'spec/**/*_spec.rb']
end
